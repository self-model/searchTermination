---
title: "Untitled"
author: "xx"
date: "28/06/2019"
output: html_document
---

```{r}
library(reticulate)
py_run_string("import os as os")
```

```{python}
import json
import pandas as pd
import numpy as np
# np.warnings.filterwarnings('ignore')
# import difflib
# #notebook 
# # from IPython.core.debugger import set_trace
from os import path as path
# import pdb
# %matplotlib inline
```

```{python}
def to_csv(filename):
    dfs=[]
    with open(filename+'.txt') as json_file:
        for i,line in enumerate(json_file):
            if line[0:14]!='Consent given.':
                dfs.append(pd.read_json(line))
            

    group_df = pd.concat(dfs)
    group_df.to_csv(filename+'.csv', index=False)
    return(group_df)
    
group_df = to_csv(path.join('..','experiments','pilot','jatos_results_batch1'))
group_df[group_df.trial_type=='survey-text'].responses
```